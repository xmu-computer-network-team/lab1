import common.config
import common.pattern

class FrameBuilder:
    def _init_(self):
    

    def draw_finder(self):
        #外黑内白中间3x3黑
        pattern = generate_finder_pattern()
        for r in range(FINDER_SIZE)
        